#include <stdio.h>

/**
* print_alphabet - Prints the alphabet in lower case
*
* Return: Always 0 (Success)
*/
int main()

{
	int ch;

	for ( ch= 'a'; ch<= 'z'; ch++ )
	
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

